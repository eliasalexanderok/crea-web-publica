# Config local de previsualización (Claude Code) — no va al repo público
.claude/

# Fotos fuente en alta (originales) — se convierten a webp; no van al deploy
assets/hd/

# Sistema operativo
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Temporales
*.opt.mp4
*.tmp
*~
